{
  "name": "mineguard-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd dashboard && yarn install && node build-vercel.js",
    "build:vercel": "cd dashboard && yarn install && node build-vercel.js",
    "build:gh-pages": "cd dashboard && yarn install && node build-gh-pages.js"
  }
}
